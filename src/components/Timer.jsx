import React from "react";

const Timer = (props) => {

  return (
    <div className='timer-box'>
      {/* Timer must take the time value from the first task in the tasks array */}
      <h1 className='timer'></h1>
    </div>
  )
}

export default Timer;